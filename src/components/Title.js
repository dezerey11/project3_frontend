import { Link } from "react-router-dom";

function Title(props) {
    return (
      <div className="title">
        <br />
        <h1>Workout Tracker</h1>
        <br />
      </div>
    );
  }

export default Title;